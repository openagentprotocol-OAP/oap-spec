{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://openagentprotocol.eu/schemas/v1.0/oap-conformance-receipt.schema.json",
  "title": "OAP Conformance Receipt",
  "description": "A signed attestation that a specific implementation, identified by its DID, has executed the OAP Conformance Test Suite at a pinned version against a pinned target and has demonstrated conformance with the listed Conformance Levels. Conformance Receipts are referenced from a Manifest through the conformance.receipt_uri field and are verifiable independently by any consuming Agent through the procedure described in RFC 0019 section 6.",
  "type": "object",
  "required": [
    "receipt_id",
    "type",
    "timestamp",
    "implementation",
    "suite",
    "target",
    "claimed_levels",
    "results_summary",
    "fixtures_hash",
    "results_hash",
    "signatures"
  ],
  "properties": {
    "receipt_id": {
      "type": "string",
      "pattern": "^urn:oap:conformance:[A-Za-z0-9_-]+$",
      "description": "Unique identifier for this conformance receipt."
    },
    "type": {
      "type": "string",
      "const": "conformance"
    },
    "timestamp": {
      "type": "string",
      "format": "date-time"
    },
    "implementation": {
      "type": "object",
      "required": ["did", "name", "version"],
      "properties": {
        "did": { "type": "string", "description": "DID of the implementation that ran the suite." },
        "name": { "type": "string" },
        "version": { "type": "string" },
        "build_hash": { "type": "string", "description": "Optional Git commit or container hash of the build under test." },
        "deployment_uri": { "type": "string", "format": "uri", "description": "Optional public URI under which the tested deployment is reachable." }
      }
    },
    "suite": {
      "type": "object",
      "required": ["name", "version", "spec_version"],
      "properties": {
        "name": { "type": "string", "const": "oap-conformance-test-suite" },
        "version": { "type": "string", "description": "Semantic version of the test suite that was executed." },
        "spec_version": { "type": "string", "description": "Version of the OAP specification the suite tests, for example 1.0." },
        "suite_repo": { "type": "string", "format": "uri" },
        "suite_commit": { "type": "string", "description": "Git commit hash of the test suite at execution time." }
      }
    },
    "target": {
      "type": "object",
      "required": ["uri"],
      "properties": {
        "uri": { "type": "string", "format": "uri" },
        "manifest_hash": { "type": "string", "description": "SHA-256 hash of the manifest fetched from the target during the run." }
      }
    },
    "claimed_levels": {
      "type": "array",
      "items": { "type": "string", "pattern": "^(L[0-5]|W[1-3]|C[1-3])$" },
      "minItems": 1,
      "description": "Conformance Levels that the implementation claims to satisfy on the basis of this run."
    },
    "results_summary": {
      "type": "object",
      "required": ["total", "passed", "failed"],
      "properties": {
        "total": { "type": "integer", "minimum": 0 },
        "passed": { "type": "integer", "minimum": 0 },
        "failed": { "type": "integer", "minimum": 0 },
        "skipped": { "type": "integer", "minimum": 0 }
      }
    },
    "results_uri": {
      "type": "string",
      "format": "uri",
      "description": "Optional URI at which the full results JSON can be downloaded for independent verification."
    },
    "fixtures_hash": {
      "type": "string",
      "pattern": "^sha256:[0-9a-f]{64}$",
      "description": "Hash of the concatenated fixture file paths and contents used during the run."
    },
    "results_hash": {
      "type": "string",
      "pattern": "^sha256:[0-9a-f]{64}$",
      "description": "Hash of the canonical JSON serialization of the full results object."
    },
    "attestor": {
      "type": "object",
      "description": "Optional independent attestor who witnessed or re executed the run.",
      "properties": {
        "did": { "type": "string" },
        "name": { "type": "string" },
        "method": { "type": "string", "enum": ["self", "third_party_replay", "live_witness", "registry"] }
      }
    },
    "validity": {
      "type": "object",
      "properties": {
        "not_before": { "type": "string", "format": "date-time" },
        "not_after": { "type": "string", "format": "date-time", "description": "Receipts SHOULD declare an expiry, typically 90 days after issuance." }
      }
    },
    "previous_receipt_hash": {
      "type": "string",
      "description": "Hash of the previous Conformance Receipt issued for the same implementation, enabling a per implementation chain. Use 'genesis' for the first receipt."
    },
    "signatures": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": ["by", "alg", "value"],
        "properties": {
          "by": { "type": "string" },
          "alg": { "type": "string", "enum": ["EdDSA", "ES256", "ES384", "RS256", "PQ-Dilithium3"] },
          "value": { "type": "string" }
        }
      }
    }
  }
}
